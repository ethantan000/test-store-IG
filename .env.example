# Database connection
DATABASE_URL="file:./dev.db"

# Stripe configuration (replace with your real keys)
STRIPE_SECRET_KEY=sk_test_yourkey
STRIPE_WEBHOOK_SECRET=whsec_yourwebhooksecret

# Application URL
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Admin token used to log in to the admin dashboard (/admin?token=TOKEN)
NEXT_PUBLIC_ADMIN_TOKEN=replace_me_with_a_secure_token

# Email (SMTP) configuration. Replace with your provider's details or a transactional email service.
EMAIL_HOST=smtp.example.com
EMAIL_PORT=587
EMAIL_USER=username
EMAIL_PASS=password
EMAIL_FROM=no-reply@viralgoods.example

# Comma‑separated list of admin emails permitted to receive order notifications
ADMIN_EMAILS=admin@viralgoods.example